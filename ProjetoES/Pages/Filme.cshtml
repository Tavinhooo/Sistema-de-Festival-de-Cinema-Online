@page
@model FilmeModel
@{
    ViewData["Title"] = "Detalhes do Filme";
}

@section Styles {
    <link rel="stylesheet" href="~/css/filme.css" />
    <link rel="stylesheet" href="~/css/modais.css" />
}

<div class="main-content">
    <div class="movie-hero">
        <div class="movie-info-col">
            <div class="movie-header">
                <div class="movie-poster">
                    <img src="@Model.Filme!.PosterUrl" style="width:100%; border-radius:15px;" />
                </div>
                <div class="movie-details">
                    <h1>@Model.Filme!.Titulo</h1>
                    <p>⭐ 8.1 (Rating)</p> <p>@Model.Filme!.Genero</p>
                    <p>@Model.Filme!.Ano &nbsp;&nbsp; @Model.Filme!.DuracaoMinutos min &nbsp;&nbsp; +16</p>
                    <h3 style="color: #6b439b; margin-top: 10px;">@Model.Filme!.PrecoBilhete.ToString("0.00")€</h3>
                </div>
            </div>

            <div class="movie-buttons">
                <button class="btn-pill">Sinopse</button>
                <button class="btn-pill">Elenco e Realizador</button>
                <button class="btn-add-cart">Adicionar ao carrinho</button>
            </div>
        </div>

        <div class="movie-trailer-col">
            <h2 class="trailer-title">Trailer:</h2>
            <div class="trailer-box">
                <i class="far fa-play-circle"></i>
            </div>
            
            <div class="trailer-actions">
                <div class="action-item">
                    <i class="fas fa-plus-circle"></i> Ver Depois
                </div>
                <div class="action-item" id="btnAbrirAvaliacao" style="cursor: pointer;">
                    <i class="far fa-star"></i> Avaliar
                </div>
                <div class="action-item">
                    <i class="far fa-eye"></i> Visto
                </div>
            </div>
        </div>
    </div>

    <div>
        <div class="section-badge">Relacionados</div>
        <div class="card-row">
            @for (int i = 0; i < 5; i++)
            {
                <div class="card">
                    <div class="shapes-placeholder">
                        <div style="width: 100%; display:flex; justify-content:center;">
                            <div class="shape-triangle"></div>
                        </div>
                        <div class="shape shape-star"></div>
                        <div class="shape shape-square"></div>
                    </div>
                </div>
            }
        </div>
    </div>

    <div>
        <div class="section-badge">Avaliações</div>
        <div class="reviews-row">
            @for (int i = 0; i < 4; i++)
            {
                <div class="review-card">
                    <div class="review-header" style="justify-content: space-between; width: 100%;">
                        <div style="display: flex; align-items: center; gap: 10px;">
                            <span>John Jones</span>
                            <i class="far fa-star"></i> 9
                        </div>
                        <button class="btn-reportar-comentario" style="background:none; border:none; color:#bbb; cursor:pointer;" title="Denunciar">
                            <i class="fas fa-flag"></i>
                        </button>
                    </div>
                    <div class="review-body">
                        Comentario: Este filme é excelente, recomendo vivamente a todos!
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<div class="modal-overlay" id="modalAvaliacao">
    <div class="modal-box">
        <div class="modal-header">
            <h2>Avaliar Filme</h2>
            <button class="btn-close" type="button"><i class="fas fa-times"></i></button>
        </div>
        <p style="font-size: 14px; color: #666;">Qual é a tua nota para o <strong>Título do Filme</strong>?</p>
        <div class="rating-scale">
            <button class="rating-btn">1</button>
            <button class="rating-btn">2</button>
            <button class="rating-btn">3</button>
            <button class="rating-btn">4</button>
            <button class="rating-btn">5</button>
            <button class="rating-btn">6</button>
            <button class="rating-btn">7</button>
            <button class="rating-btn">8</button>
            <button class="rating-btn">9</button>
            <button class="rating-btn">10</button>
        </div>
        <div class="modal-footer">
            <button class="btn-cancel" type="button">Cancelar</button>
            <button class="btn-submit" type="button">Submeter Avaliação</button>
        </div>
    </div>
</div>

<div class="modal-overlay" id="modalDenuncia">
    <div class="modal-box">
        <div class="modal-header">
            <h2>Denunciar Comentário</h2>
            <button class="btn-close" type="button"><i class="fas fa-times"></i></button>
        </div>
        <div class="form-group">
            <label>Motivo da Denúncia</label>
            <select>
                <option value="">Selecione um motivo...</option>
                <option value="spam">Spam ou Publicidade</option>
                <option value="ofensivo">Linguagem Ofensiva</option>
                <option value="spoiler">Contém Spoilers</option>
                <option value="outro">Outro motivo</option>
            </select>
        </div>
        <div class="form-group">
            <label>Detalhes Adicionais (Opcional)</label>
            <textarea placeholder="Explique brevemente porque este comentário viola as regras..."></textarea>
        </div>
        <div class="modal-footer">
            <button class="btn-cancel" type="button">Cancelar</button>
            <button class="btn-submit" type="button" style="background-color: #e53935;">Denunciar</button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const btnAvaliar = document.getElementById('btnAbrirAvaliacao');
            const btnsDenunciar = document.querySelectorAll('.btn-reportar-comentario');
            const modalAvaliacao = document.getElementById('modalAvaliacao');
            const modalDenuncia = document.getElementById('modalDenuncia');
            const botoesFechar = document.querySelectorAll('.btn-close, .btn-cancel');

            if(btnAvaliar) {
                btnAvaliar.addEventListener('click', function() {
                    modalAvaliacao.classList.add('active');
                });
            }

            btnsDenunciar.forEach(btn => {
                btn.addEventListener('click', function() {
                    modalDenuncia.classList.add('active');
                });
            });

            botoesFechar.forEach(btn => {
                btn.addEventListener('click', function() {
                    modalAvaliacao.classList.remove('active');
                    modalDenuncia.classList.remove('active');
                });
            });
            
            const ratingBtns = document.querySelectorAll('.rating-btn');
            ratingBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    ratingBtns.forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                });
            });
        });
    </script>
}